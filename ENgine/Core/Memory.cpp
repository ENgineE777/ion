
//#include "Core/core.h"

/*
inline void* operator new(size_t size)
{
	return core->Memory()->AllocMem(size, __FILE__, __LINE__);
}

void* operator new[](size_t size)
{
        return 0;//core->Memory()->AllocMem(size, __FILE__, __LINE__);
}

inline void operator delete[](void* p)
{
	core->FreeMem(p);
}

inline void operator delete[](void* p, size_t size)
{
	core->FreeMem(p);
}*/